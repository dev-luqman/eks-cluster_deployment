## MongoDb and MongoDb_express deployment to aws kubernetes

# Commands
base 64 encoded value command
``` echo -n "username" | base64 ```

kubectl command for deployment 
``` kubectl apply -f <filename>